# Memory Graph Validation Baseline — 2026-08-25

## Scope

This report was generated by the read-only graph validator for the current local user graph. The scan queried only Neo4j and did not create, update, merge, invalidate, or delete any node or relationship.

## Baseline

| Metric | Result |
| --- | ---: |
| Entity nodes | 33 |
| `RELATION` edges | 25 |
| Errors | 0 |
| Warnings | 2 |

## Findings

1. `SELF_MISSING` — no explicit canonical self entity was found.
2. `ALIAS_ENTITY_PRESENT` — one standalone `称呼别名` entity exists.

The validator also emitted Neo4j notifications that lifecycle and identity properties are absent on legacy nodes. The validator treats missing `is_active` and `is_invalidated` as active for backward compatibility; it does not infer identity from a display name.

## Next migration gate

Before changing existing data, generate a per-node migration preview that proposes:

- the canonical self node and its stable `identity_key`;
- aliases to move into `self.aliases`;
- source entities and relationships that would be preserved or logically inactivated;
- conflicts requiring explicit user confirmation.

After review, rerun this validator and require zero `SELF_MISSING` and zero unresolved alias-entity findings before enabling automatic identity curation.
